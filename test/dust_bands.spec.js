var expect = require('chai').expect;
var DustBands = require('../src/dust_bands');

describe('DustBands',function(){
  // before(function(done){
  //
  // });

  it('should be defined',function(){
    expect(DustBands).to.be.defined;
  });

});
